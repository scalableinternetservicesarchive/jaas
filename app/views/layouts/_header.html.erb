<header class="header">
	<% if !current_page?(root_path) %>
		<%= link_to 'Stranger Arranger', root_path %>
	<% end %>

	<% if user_signed_in? %>
	<div id="headerLogout">
		<%= link_to 'Logout', destroy_user_session_path, class: "btn btn-5", id: "headerLogOutButton", :method => :delete %>
	</div>
	<% end %>
</header>